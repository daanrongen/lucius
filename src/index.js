import React from "react";
import { render } from "react-dom";
import "./stylesheets/ui.scss";

// window.React = React;
//
// render(<SkiDayCount />, document.getElementById("react-container"));

ReactDOM.render(component, document.getElementById("location"));
